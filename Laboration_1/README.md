# Laboration 1 – Movie Recommendation System

This project implements a hybrid movie recommendation system using the MovieLens dataset.

## Description

The system recommends 5 movies similar to a given input movie by combining:

- Content-based filtering (genres + tags)
- Collaborative filtering (user ratings)

## How to run

Make sure all files are in the same folder, then run:

```bash
python main.py